package middleware

import (
	"github.com/gin-gonic/gin"
)

func EzbAuthbasic(c *gin.Context) {

	//config, _ := c.Keys["configuration"].(confmanager.Configuration)
	//expath := c.GetString("exPath")

}
